#include "main.h"

/**
 * _strcat - function that concatenates two strings.
 * @dest: first string
 * @src: second string
 * Return: concatenated string
 */
char *_strcat(char *dest, char *src)
{
	int a, b;

	for (a = 0; dest[a] != '\0'; a++)
	{
	}

	for (a = 0; (dest[a + b] = *src++) != '\0'; b++)
	{
	}

	return (dest);
}
